% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior_predictive_checks.R
\name{posterior_predictive_checks}
\alias{posterior_predictive_checks}
\title{Posterior Predictive Checks for Different Model Types}
\usage{
posterior_predictive_checks(fit, stan_data, config, outdir = NA)
}
\arguments{
\item{fit}{A Stan model fit object.}

\item{stan_data}{A list or data frame containing the data used in the
model fitting process. The structure should align with what is expected
by the specific model type.}

\item{config}{A configuration list.}

\item{outdir}{An optional directory path as a character string where the
output will be saved. If \code{NA} (default), the function emits a warning and
does not save the output.}
}
\value{
Returns a data table with the results of the posterior predictive checks.
The exact structure of this table depends on the type of model used.
}
\description{
This function performs posterior predictive checks on a fitted Stan model object.
It supports different types of models (zero-inflated, Poisson, etc.) and
provides a warning for unsupported model types.
}
\details{
The function checks the \code{model$name} field in the \code{config} parameter
to decide which type of posterior predictive check to perform. Currently,
it supports zero-inflated models (prefix "zi"), Poisson models (excluding
those with prefixes "pois" and "rsb"), and has a placeholder for logistic
regression models (prefix "logit"), which is not yet implemented.
}
